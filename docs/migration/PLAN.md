# PLAN.md — Миграция Recenza в Claude Code

Фазовый план миграции дизайн-прототипа **Recenza** (многоглавный девблог с редакционным
ревью) в поддерживаемый **production-монолит на Next.js 16**.

**Как читать:** каждая фаза — `Название · Цель · Todo · DoD`. Фаза закрыта только когда выполнен
весь DoD. Сборка `npm run build` — необходимое, прохождение тестов на тестовом стенде —
достаточное условие готовности фазы.

**Сопутствующие документы:** `PROMPT.md` (промт запуска), `CLAUDE.md` (целевая архитектура),
`ENVIRONMENTS.md` (стенды + флоу БД), `TESTING.md` (тест-кейсы + Playwright).

**Принципы на весь проект:**
- Монолит. Один Next.js-репозиторий, без выделенных сервисов.
- Доменная модель — **глава-ориентированная** (Blog → Chapter → Revision → blocks), по `README.md` прототипа.
- Два стенда обязательны: **тестовый** (3001, `blog.test.db`) и **продовый** (Turso/Vercel).
- Все БД — миграциями Drizzle. Timestamps — Unix seconds. ID — `ulid()`. JSON — только в `try/catch`.
- Интерфейс на русском. Эстетика: Lora/Literata/Fira Code, teal-акцент, тонкие границы, без теней.

**Карта фаз:**

| # | Фаза | Слой |
|---|------|------|
| 0 | Bootstrap и архитектура Claude Code | Инфраструктура |
| 1 | Доменная модель и схема БД | Данные |
| 2 | Два стенда + seed + флоу БД | Инфраструктура |
| 3 | Auth, роли, ролевой гейтинг | Платформа |
| 4 | Читательский слой (публичный) | Продукт |
| 5 | Авторский слой: кабинет + блочный редактор | Продукт |
| 6 | Редакционный review-flow (ReviewPage) | Продукт |
| 7 | Комментирование (читатель↔автор↔читатель) | Продукт |
| 8 | Портфолио «Об авторе» | Продукт |
| 9 | Админка и модерация | Продукт |
| 10 | Тест-кейсы и пользовательские сценарии | Качество |
| 11 | Playwright: MCP-исследование + TS-автотесты | Качество |
| 12 | Hardening: security / a11y / SEO / design / perf | Качество |
| 13 | Прод-деплой | Релиз |

---

## Фаза 0 — Bootstrap и архитектура Claude Code

**Цель.** Завести поддерживаемый каркас монолита и всю «обвязку Claude Code» (CLAUDE.md, правила,
сабагенты, скиллы, Playwright MCP), чтобы дальше работать дисциплинированно и проверяемо.

**Todo.**
- [ ] Инициализировать Next.js 16 (App Router) + TypeScript + Tailwind CSS v4. Path alias `@/* → src/*`.
- [ ] Установить ядро: `@libsql/client` + `drizzle-orm` + `drizzle-kit` (dialect `turso`), `iron-session`,
      `bcryptjs`, `ulid`, `next-mdx-remote/rsc` + `rehype-pretty-code` (Shiki), `next-themes`; dev: `dotenv-cli`.
- [ ] Положить `CLAUDE.md` (из этого пакета) в корень. Создать `.claude/rules/`:
      `security.md`, `next-app-router.md`, `drizzle-queries.md`, `mdx-components.md`, `frontend-design.md`.
- [ ] Перенести сабагентов в `.claude/agents/`: `playwright-tester`, `code-reviewer`, `security-reviewer`,
      `design-watcher`, `seo-optimizer` (адаптировать терминологию «статья»→«глава/ревизия» по
      `docs/prototype/legacy-article-model-CLAUDE.md`).
- [ ] Перенести скиллы в `.claude/skills/`: `qa-test-planner`, `playwright-best-practices`; создать
      `next-best-practices`.
- [ ] Настроить **Playwright MCP** (`.mcp.json` / конфиг Claude Code) — проверить, что `mcp__playwright__*`
      доступны.
- [ ] Завести дизайн-токены в `src/app/globals.css`: шрифты (Lora/Literata/Fira Code через `next/font`,
      subsets `latin`+`cyrillic`), teal-акцент, семантические цвета, поверхности, тёмная/светлая темы,
      анимации только на `transform`/`opacity` + `prefers-reduced-motion`.
- [ ] `.env.example` со всеми переменными (см. `ENVIRONMENTS.md`). README репозитория с командами.

**DoD.**
- `npm run dev` поднимает пустой каркас, `npm run build` зелёный, `npm run lint` чистый.
- В корне есть `CLAUDE.md`; `.claude/{rules,agents,skills}` заполнены и валидны.
- Playwright MCP отвечает (тестовый `browser_navigate` на `about:blank` проходит).
- Дизайн-токены подключены, переключатель темы работает, hardcoded-цветов нет (проверено `design-watcher`).

---

## Фаза 1 — Доменная модель и схема БД

**Цель.** Перенести глава-ориентированную модель прототипа (`README.md` §1–2, §8) в схему Drizzle
со всеми таблицами и сгенерировать миграции.

**Todo.**
- [ ] `src/lib/db/schema.ts` — таблицы (полный список и поля — в `ENVIRONMENTS.md` §«Схема БД»):
      `users`, `blogs`, `chapters`, `chapter_revisions`, `chapter_reviewers`, `reviewer_history`,
      `threads`, `thread_replies`, `review_chat`, `review_checklists`,
      `public_comments`, `comment_votes`, `chapter_votes`, `bookmarks`, `follows`, `notifications`,
      `portfolios`, `reports`, `primary_change_requests`, `removed_reviewers`.
- [ ] Зафиксировать перечисления: `role` (`reader|author|reviewer|admin`),
      `revision.status` (`draft|under-review|changes-requested|published`),
      `verdict` (`approve|request-changes`), `thread.status` (`open|resolved`),
      `complexity` (`simple|medium|complex`),
      `block.type` (p/h2/h3/quote/list/code/callout/mermaid/latex/image/table/embed — **12 типов**).
- [ ] Блоки главы — `JSONB`-массив в `chapter_revisions.blocks`; снапшот последней публикации — `prev_blocks`.
      Статус блока (added/edited) для диффа НЕ хранится — вычисляется из `prev_blocks` (`diffWords`).
- [ ] FK **только на суррогатные id** (`users.id`/`chapters.id`/`blogs.id`; `handle`/`slug` мутабельны →
      денормализуются, не ключи). `public_comments`/`removed_reviewers` ключевать `chapter_id`(+`revision_number`).
      Каскады + `PRAGMA foreign_keys=ON` на каждом соединении (`db/index.ts`; в libsql FK по умолчанию off).
      Engagement-таблицы — `uniqueIndex` для race-safe toggle. `users.pinned_blog_id→blogs.id` (закреплённый блог).
- [ ] `src/lib/db/index.ts` — libsql-клиент (env Turso → fallback `file:blog.db`). `drizzle.config.ts`.
- [ ] `src/types/index.ts` — общие типы (`UserRole`, `RevisionStatus`, `Verdict`, `BlockType`, `ApiError`, …).
- [ ] `npx drizzle-kit generate` → миграции в репозитории.

**DoD.**
- `drizzle-kit generate` создаёт миграции без ошибок; `drizzle-kit migrate` применяет их на чистый `file:`.
- Схема покрывает **всю** модель из `README.md` (главы, ревизии, треды/правки/чат, кредит ревьюеров
  по версиям, комментарии с привязкой к блоку и ревизии, портфолио, жалобы, смена ведущего, пин блога).
- `PRAGMA foreign_keys=ON` включается в `db/index.ts`; каскады реально срабатывают на `file:` БД (проверено
  удалением блога/главы). FK ведут на суррогатные id, не на `handle`/`slug`.
- `code-reviewer` не находит P0/P1; raw SQL отсутствует; все timestamps — Unix seconds.

---

## Фаза 2 — Два стенда + seed + флоу БД

**Цель.** Поднять **тестовый** и **продовый** стенды, создать все БД и детерминированный seed,
покрывающий все роли/статусы/сценарии. Полностью реализовать `ENVIRONMENTS.md`.

**Todo.**
- [ ] Окружения: `.env.local` (dev, 3000, `blog.db`), `.env.test` (test, 3001, `blog.test.db`,
      `ADMIN_PASSWORD_PLAIN`), прод-переменные (Turso/Vercel). Описать в `.env.example`.
- [ ] Скрипты `package.json`: `dev`, `dev:test`, `build`, `lint`, `seed`, `seed:test`,
      `test:reset`, `test:e2e`, `test:e2e:ui`, `test:e2e:report`.
- [ ] `src/lib/db/seed.ts` + `seed-test.ts` — **детерминированный** датасет: пользователи всех 4 ролей,
      блоги/главы во **всех** статусах (черновик, на ревью, запрошены правки, опубликовано),
      ревизии с `prev_blocks` для диффа, треды (open/resolved) с suggestion, сообщения чата,
      кредит ревьюеров по двум версиям, публичные комментарии (включая к старой ревизии), портфолио,
      жалоба, заявка на смену ведущего. Seed завершается `process.exit()`.
- [ ] Скрипты тест-стенда в `.claude/playwright-tester/`: `reset-test-db.sh`, `healthcheck.sh`,
      `login.sh`, `api-check.sh`, `db-query.sh`, `session-manager.sh`, `cleanup-test-data.sh`
      (адаптировать имена таблиц под главы).
- [ ] Прод-флоу БД: `drizzle-kit migrate` против Turso; bootstrap-админ через env, без self-registration.

**DoD.**
- `npm run dev:test` поднимает стенд на 3001 на `blog.test.db` с применённым seed; dev-стенд (3000/`blog.db`) не затронут.
- `npm run test:reset` возвращает тестовую БД к фиксированному состоянию идемпотентно.
- Тестовые аккаунты (`reader/author/reviewer` = `password`, admin из `.env.test`) логинятся.
- `db-query.sh` показывает данные во всех статусах; seed детерминирован (повтор даёт тот же снимок).
- Прод-миграции применяются к Turso в сухом прогоне без ошибок.

---

## Фаза 3 — Auth, роли, ролевой гейтинг

**Цель.** iron-session-аутентификация, 4 роли и **binding-гейтинг** ответственности из спецификации.

**Todo.**
- [ ] `src/lib/auth.ts`: `SessionData {isAdmin, userId?, userRole?}` (инвариант: admin и userId не одновременно);
      `getSession`, `requireUser(role?)`, `requireAuthor`, `requireReviewer`, `requireAdmin`.
- [ ] Route-группы: `app/admin/(protected)/`, `app/author/(protected)/`, `app/reviewer/(protected)/`,
      `app/(reader)/`, публичный сегмент. Layout каждой группы вызывает свой `require*`.
- [ ] Эндпоинты: `POST/DELETE /api/auth` (admin), `POST/DELETE /api/auth/user` (пользователи),
      `GET /api/auth/user`. Rate-limit логина (5/15мин по `x-forwarded-for`), CSRF same-origin на мутациях.
- [ ] **Ролевой гейтинг (binding):** читатель комментирует везде; автор видит/читает/комментирует
      **только свои** блоги (чужие фильтруются из ленты/каталога и блокируются в ридере); ревьюер
      никогда не комментирует и не ведёт блоги; админ модерирует. Централизовать проверки.

**DoD.**
- Без сессии `/admin`, `/author`, `/reviewer` → редирект; чужой контент → 403.
- Rate-limit логина: 6-я неудача → 429. Пароли — bcrypt; секреты только из env.
- Гейтинг проверен по матрице ролей (тест-кейсы фазы 10): ни одна роль не делает запрещённого.
- `security-reviewer`: 0 критических.

---

## Фаза 4 — Читательский слой (публичный)

**Цель.** Публичное чтение многоглавных блогов + читательский engagement-слой.

**Todo.**
- [ ] `HomeScreen`/`ReaderFeed` (лента, фильтры-чипы), `ArticleIndex` (каталог карточек блогов).
- [ ] `BlogReader`: широкая колонка чтения + правый `SeriesNav` (список глав, вложенный ToC активной
      главы; одна глава → только ToC), прогресс, режим **«Весь блог»**. Рендер всех типов блоков
      (p/h2/h3/quote/list/code/callout/mermaid/image/table/embed) идентично ревью-виду.
- [ ] Реакции (**только роль `reader`**; author/reviewer не голосуют/не закладывают/не подписываются):
      голоса (±1), закладки — race-safe `db.transaction()`, `uniqueIndex`, rate-limit. `BookmarksScreen`.
      Подписки (follow) + лента по подпискам (`/reader`).
- [ ] Уведомления: polling-бейдж (новые главы в подписках + «ваш ход» в ревью), read-state.
- [ ] Кредит ревьюеров в конце главы: текущие чипами, прошлые версии — за раскрытием.
- [ ] SEO/Feed: `generateMetadata`, OG, `/feed.xml`, `/sitemap.ts`, `/robots.ts`, JSON-LD.

**DoD.**
- Открытие разных блогов рендерит **разный** контент; `document.title`/OG обновляются (регресс-ловушка из README §3).
- Гость, голосуя/закладывая/подписываясь/комментируя, уходит на логин; intent (`vote`/`bookmark`/`follow`/`comment`) реплеится после входа.
- Закладки/голоса/подписки идемпотентны и race-safe; гонок нет (тест на дабл-клик).
- Lighthouse/`seo-optimizer`: у каждой публичной страницы уникальные title/description/OG.

---

## Фаза 5 — Авторский слой: кабинет + блочный редактор (Variant B)

**Цель.** Кабинет автора, управление многоглавными блогами и блочный редактор «writing-first».

**Todo.**
- [ ] `AuthorPortal` (карточки блогов; плитка «создать» первой; пин блога — `users.pinned_blog_id`
      (один на автора) — сортирует вперёд + кольцо, всплывает на публичном профиле).
      `BlogDetail` (список глав блога: превью/+глава; тумблер пина блога).
- [ ] `Editor` (Variant B): минимальный топбар (save-state, превью, split-preview ≥lg, ⚙ настройки,
      «Отправить на ревью →»); тело-документ (хлебные крошки → авто-растущий заголовок → dashed-чип
      строки настроек → блоки); слэш-меню (`/`, 4 группы, **12 типов блоков / 14 пунктов**); markdown-шорткаты; инлайн-тулбар
      выделения (B/I/Code/Link); левый рельс-гаттер (add/drag).
- [ ] `ChapterSettingsPopover` — только метаданные публикации (slug авто+override, теги, обложка).
- [ ] `SubmitSheet` (правая шторка): чек-лист готовности (гейт), уровень сложности, ревьюеры + выбор
      ведущего, заметка ревьюерам. Submit заблокирован, пока не пройден гейт. **Без поля дедлайна**
      (намеренно: README §5; в коде прототипа `Editor3.jsx` дедлайн есть — **не переносить**, в схеме его нет).
      Новая глава префиллит ревьюеров из последней отрецензированной главы блога.
- [ ] Версионирование: при `PUT` главы — снапшот предыдущей ревизии перед обновлением.

**DoD.**
- Автор создаёт блог → главу → пишет блоки → сохраняет черновик → отправляет на ревью; статус главы меняется.
- Портфолийные главы «Об авторе» минуют `SubmitSheet` (публикуются без ревью) — задел для фазы 8.
- `design-watcher`: редактор соответствует токенам; хит-таргеты ≥36/44px; dark mode целостен.
- Автор не видит/не редактирует чужие блоги (ownership на всех `/api/author/*`).

---

## Фаза 6 — Редакционный review-flow (ReviewPage, Concept C)

**Цель.** Самый сложный экран: двухколоночное ревью с тредами, баблами, предложениями правок,
вердиктами, чатом сессии и кросс-экранной синхронизацией статусов. Сердце продукта.

**Todo.**
- [ ] Модель ревью: назначения ревьюеров на главу, **ведущий (primary)**, вердикты на ревьюера/ревизию
      (`chapter_reviewers`), `reviewer_history` (кредит по ревизиям), **чат сессии** (`review_chat`, вне
      тредов), чек-лист готовности. Per-block `BlockVerdictStamp` (approve/fix/discuss) — **эфемерный UI**,
      не персистится; статус блока (added/edited) — вычисляется из `prev_blocks` (`diffWords`), не хранится.
- [ ] `ReviewHeaderV2`: топбар (назад / составной тайтл Блог→Глава / ревизия / статус / выбор POV /
      триггер модалки команды); strip глав (`role="tablist"`); presence-strip (онлайн-точки).
- [ ] `ConvoCanvas`: колонка статьи (инлайн-дифф `diffWords(prev,curr)`, правый гаттер: **bauble**
      со счётчиком открытых + `BlockVerdictStamp` циклом approve/fix/discuss; двойной клик → инлайн-правка
      для автора) + `ThreadsRail` (`VerdictLedger` 3 счётчика; `ThreadCard` с якорем-цитатой, suggestion-диффом,
      вложенными ответами; композер с индикатором набора).
- [ ] **Двунаправленный sync** клика: bauble↔thread (scroll + flash, мобильные табы). **Apply-and-close**:
      `chapter.blocks[i].text` ← replace(suggestion), тред → resolved, бродкаст стора.
- [ ] `ActionBar` (sticky): POV ревьюера (Нужны правки / Одобрить — только когда `under-review`);
      POV автора (Сменить ведущего / Опубликовать при всех approve / Отправить v{N+1}).
- [ ] **Чат сессии (`review_chat`)** — отдельная панель/вкладка рядом с тредами (UX-эталона в прототипе
      НЕТ, см. REVIEW-REPORT P2-10): хронологический список сообщений сессии, видимый всем участникам
      (ревьюеры + автор + админ), presence/typing; не смешивается с привязанными к блокам тредами.
- [ ] `PrimaryChangeModal`, `TeamSheet` (мобайл), `Toast` (`aria-live`). Кросс-экранный стор статусов
      (на проде — серверное состояние; задел под websocket вместо fake-presence).
- [ ] Указание ревьюеров в опубликованной главе **по версиям** (связь с фазой 4 кредитом).

**DoD.**
- Полный цикл: автор отправляет v1 → ревьюер открывает тред с suggestion → автор «применить и закрыть» →
  ревьюер ставит approve → автор публикует. Статус синхронно меняется во всех экранах (инбокс/кабинет/ридер).
- Ведущий ревьюер назначается/меняется; вердикты считаются; публикация доступна только при всех approve
  (или force-approve админом, фаза 9).
- Ревьюер не может комментировать как читатель; автор не ставит вердикты. Мобильные табы Статья/Обсуждения работают.
- Опубликованная глава показывает корректный список ревьюеров (текущие + прошлые версии).

---

## Фаза 7 — Комментирование (читатель ↔ автор ↔ читатель)

**Цель.** Публичный слой комментариев с привязкой к блокам и ревизиям, окном редактирования и
ролевым гейтингом. Особый сценарий: диалог **читатель → автор → читатель**.

**Todo.**
- [ ] `public_comments`: ключ `blogSlug+chapterSlug+revision`, опц. `anchor {blockId, quote}` (скролл к
      `id="block-<id>"`), `editedAt`, `parentId` (вложенность ≤2), `deletedAt` (soft delete).
- [ ] `CommentsSection`: фильтр к открытой главе; комментарии к **старой** ревизии — в сворачиваемый
      спойлер «прошлые версии» (бейдж «к версии vN»); окно правки 15 мин для комментариев и ответов;
      клик по цитате — скролл к блоку. Новый комментарий наследует текущую главу+ревизию.
- [ ] **Гейтинг:** комментируют только читатели (и автор — в своих блогах как участник диалога);
      ревьюеры не комментируют; автор не комментирует чужие блоги. `commentingBlocked` блокирует.
- [ ] Голоса за комментарии (±1, race-safe; **только `reader`**). Stale-детект по номеру ревизии
      (`public_comments.revision_number` vs текущая ревизия главы).
- [ ] Уведомления: ответ автора читателю и наоборот — нить читатель↔автор↔читатель.

**DoD.**
- Сценарий «читатель спросил → автор ответил → читатель уточнил» проходит сквозным тестом; уведомления летят.
- Комментарий к старой ревизии корректно уезжает в спойлер; окно правки истекает на 16-й минуте → 403.
- Ревьюер получает 403 на POST комментария; автор — 403 на чужой блог; заблокированный — 403.
- Вложенность >2 уровней запрещена сервером.

---

## Фаза 8 — Портфолио «Об авторе»

**Цель.** Необязательное расширенное «о себе», публикуется **без ревью**.

**Todo.**
- [ ] `portfolios` (одно на автора): мини-статья из блоков, флаг видимости (show/hide), без ревизий-ревью.
- [ ] `ProfileScreen` — табы для автора **«Об авторе · Блоги»**; вход в редактор портфолио из профиля
      («Изменить портфолио») и из кабинета. У читателей/ревьюеров вкладки портфолио нет.
- [ ] Пустые состояния: гость видит только логи; владелец без портфолио — «Портфолио ещё не создано».
- [ ] Публичный профиль ревьюера: список глав, которые он рецензировал (`getReviewedChapters`).

**DoD.**
- Автор создаёт/прячет/показывает портфолио; публикация **минует** review-flow.
- Профиль ролеспецифичен: автор — табы; ревьюер — «что отрецензировал»; читатель — без портфолио.
- `design-watcher`/`seo-optimizer` чисты на странице профиля.

---

## Фаза 9 — Админка и модерация

**Цель.** Модерация, роли, баны, публикация, жалобы, смена ведущего ревьюера.

**Todo.**
- [ ] `AdminPortal`: `AdminDashboard`, `AdminUsers`/`AdminUserDetail` (создание пользователей — **только
      админ**, без self-registration; тумблеры `isBlocked` / `commentingBlocked`), `AdminReports`/
      `AdminReportDetail` (жалобы), `AdminReview` (очередь глав).
- [ ] Действия: force-approve главы (с уведомлением автора), смена ведущего ревьюера (через
      `primary_change_requests`), снятие ревьюера (`removed_reviewers` + причина), быстрый hide/show блога.
- [ ] Кросс-экранный стор: pcRequests / forced / removedReviewers — бродкаст в инбокс ревьюера,
      кабинет автора, детальный экран блога.
- [ ] Модерация комментариев (soft delete), баны.

**DoD.**
- Бан автора (`isBlocked=1`) скрывает все его блоги во всех поверхностях.
- Force-approve публикует главу и уведомляет автора; смена ведущего распространяется кросс-экранно.
- Админ **не создаёт блоги/главы**; роль пользователя не редактируется через обычный API.
- Жалоба создаётся читателем, видна админу, обрабатывается; `security-reviewer` чист.

---

## Фаза 10 — Тест-кейсы и пользовательские сценарии

**Цель.** Построить полную тестовую документацию **до** автоматизации. Используй скилл `qa-test-planner`.
Особый упор: **ревью** (глава / весь блог / чат), **публикации** (профиль / статья / глава / черновик),
**комментирование** (читатель→автор→читатель). Подробности — в `TESTING.md`.

**Todo.**
- [ ] `testing/TEST-PLAN.md` — scope, стратегия, окружения (тест-стенд!), entry/exit-критерии, риски.
- [ ] `testing/test-cases/TC-{GUEST,READER,AUTHOR,REVIEWER,ADMIN}.md` — пошаговые кейсы с ожиданиями,
      предусловиями, тест-данными, приоритетами (P0–P3).
- [ ] Сквозные **юзер-сценарии** (end-to-end), как минимум:
      - **Ревью-сценарии:** ревью одной главы (тред→suggestion→apply→approve→publish); ревью **всего блога**
        (несколько глав, разные статусы); **чат сессии** (обсуждение вне тредов, несколько ревьюеров + автор).
      - **Публикационные:** публикация черновика главы; публикация новой версии главы (v2) с обновлением
        кредита ревьюеров; публикация профиля/портфолио без ревью; публикация статьи в каталог/ленту.
      - **Комментирование:** читатель→автор→читатель (вопрос/ответ/уточнение), комментарий к старой версии,
        окно правки, гейтинг ролей, бан-комментирование.
      - **Гейтинг-матрица ролей:** что каждая роль может/не может (негативные кейсы 403/редиректы).
- [ ] `testing/smoke/SMOKE-SUITE.md` (~15 кейсов, ~15 мин) и `testing/regression/REGRESSION-SUITE.md`.
- [ ] Шаблон баг-репорта; критические инварианты (Unix seconds, ownership-403, rate-limit, XSS-MDX).

**DoD.**
- Покрыты все 4 роли + гость; матрица «роль × действие» полна (позитив и негатив).
- Явно описаны три акцентных кластера (ревью / публикации / комментирование) со сквозными сценариями.
- Каждый кейс: предусловия, шаги, ожидаемый результат, приоритет, тест-данные из seed.
- Smoke и регресс ссылаются на конкретные TC-идентификаторы.

---

## Фаза 11 — Playwright: MCP-исследование + TS-автотесты

**Цель.** Двухуровневое тестирование: **Playwright MCP** для живого исследования/верификации флоу +
**TypeScript-автотесты** (`@playwright/test`), закоммиченные и гоняемые в CI. Используй скилл
`playwright-best-practices` и сабагента `playwright-tester`.

**Todo.**
- [ ] **MCP-уровень:** через `mcp__playwright__*` пройти ключевые флоу на тест-стенде (3001), снять
      snapshot/console/network, зафиксировать реальные локаторы и тайминги — это основа для спеков.
- [ ] **TS-уровень:** `playwright.config.ts` (порт 3001, `workers:1`, `fullyParallel:false`,
      `reuseExistingServer`, репорт в `testing/reports/playwright-html`).
- [ ] `testing/e2e/global-setup.ts` — авто-генерация auth-state на роль:
      `.auth/{admin,author,reader,reviewer}.json`.
- [ ] POM/фикстуры: страницы (Reader, Editor, ReviewPage, Comments, Admin) + ролевые фикстуры.
- [ ] Спеки из тест-кейсов фазы 10: `e2e/{guest,reader,author,reviewer,admin}.spec.ts` +
      сквозные `e2e/flows/{review-chapter,review-whole-blog,session-chat,publish,comment-thread}.spec.ts`.
      Теги `@smoke`/`@critical`/`@regression`.
- [ ] CI (GitHub Actions): поднять тест-стенд, seed, прогнать `@smoke` на PR и полный регресс на ночь.
- [ ] Безопасность/реалтайм: XSS-в-MDX, CSRF, rate-limit; multi-user сценарии (автор+ревьюер в одной сессии),
      console-error monitoring.

**DoD.**
- `npm run test:e2e` зелёный на чистом seed; повтор критических (`--repeat-each=5`) стабилен (не флакает).
- Каждый акцентный сценарий фазы 10 (ревью главы/всего блога/чат, публикации, комментирование) имеет
  закоммиченный TS-спек **и** воспроизведён через MCP.
- Auth-state на 4 роли генерируется в global-setup; тесты изолированы (единый тест-стенд, sequential).
- `playwright-tester` выдаёт **GO** (все P0, ≥90% P1) на smoke и регрессе.

---

## Фаза 12 — Hardening: security / a11y / SEO / design / perf

**Цель.** Закрыть production-пробелы (`README.md` §9) и прогнать весь флот сабагентов.

**Todo.**
- [ ] Закрыть пробелы: реальный mermaid-js, KaTeX, загрузка изображений (file-input + сторедж),
      серверное review-состояние вместо in-memory стора, реальные presence/typing (websocket-задел),
      iron-session вместо демо-аккаунтов, `published_at` по ревизиям, уведомление автора о force-approve.
- [ ] `security-reviewer` (auth-bypass, инъекции, секреты, cookie, `npm audit`), `code-reviewer` (5 категорий),
      `design-watcher` (hardcoded-цвета/шрифты/тени/aria/dark mode), `seo-optimizer` (metadata/OG/sitemap/robots).
- [ ] a11y: skip-to-content, focus-management при смене главы, фокус-кольца, навигация с клавиатуры по
      баблам/тредам, high-contrast ревью, `aria-live` тосты, `role=tablist` на strip глав.
- [ ] Perf: Web Vitals/Lighthouse-бюджеты; анимации только `transform`/`opacity`; `reduced-motion`.

**DoD.**
- 0 критических у `security-reviewer`; `code-reviewer` без P0/P1 на затронутом; `design-watcher` без P0.
- `seo-optimizer`: все публичные страницы с корректными метаданными; sitemap/robots/feed валидны.
- a11y-кейсы (skip-link, focus-visible, reduced-motion, mobile-nav) проходят в Playwright.
- Известные пробелы README §9 либо закрыты, либо явно заведены в backlog с обоснованием.

---

## Фаза 13 — Прод-деплой

**Цель.** Выкатить монолит на прод-стенд (Turso + Vercel) с безопасной конфигурацией и проверками.

**Todo.**
- [ ] Прод-БД: Turso, `drizzle-kit migrate`, bootstrap-админ через env (без self-registration).
- [ ] Прод-env (Vercel): `SESSION_SECRET`, `ADMIN_PASSWORD_HASH` (`\$`-escape), `TURSO_*`, `NEXT_PUBLIC_BASE_URL`.
- [ ] Прод-проверки: RSS/sitemap/robots с прод-URL; security-заголовки; финальный `npm run build`;
      smoke на prod-preview (не на тестовом стенде!).
- [ ] Runbook отката и бэкапов; разделение секретов dev/test/prod.

**DoD.**
- Прод поднимается, миграции применены, админ заходит, гость читает опубликованные блоги.
- Smoke на prod-preview зелёный; секреты не утекли (нет в репозитории/логах).
- Тестовый и продовый стенды полностью изолированы (разные БД, env, URL).

---

## Глобальный Definition of Done (релиз)

- [ ] Все 14 фаз закрыты по своим DoD.
- [ ] Монолит собирается (`npm run build`) и проходит регресс на тестовом стенде (`playwright-tester` = GO).
- [ ] Два стенда (тест/прод) изолированы; все БД создаются миграциями; seed тест-стенда детерминирован.
- [ ] Ревью (глава/весь блог/чат), публикации (профиль/статья/глава/черновик) и комментирование
      (читатель↔автор↔читатель) покрыты тест-кейсами **и** TS-автотестами, верифицированы через Playwright MCP.
- [ ] Флот сабагентов (security/code/design/seo) чист; a11y-кейсы проходят.
- [ ] `CLAUDE.md`, `.claude/{rules,agents,skills}`, `ENVIRONMENTS.md`, `TESTING.md` актуальны.
